# -*- coding: utf-8 -*-
"""全局配置：预算、筛选阈值、评分权重、板块画像。改这里即可调智能体口味。"""

BUDGET_WAN = 30.0          # 总价硬性上限（万元）
YIELD_TARGET = 4.0         # 租售比目标线(%)，达到给满分
YIELD_MID = 2.5            # 租售比中间线(%)，低于此给低分
NET_YIELD_FLOOR = 3.5      # 净回报率门槛(%)，低于不进推荐
RENT_EASE_FLOOR = 60.0     # 出租易度分门槛，低于不进推荐
TOP_N = 6                  # 每城推荐条数
CURR_FILE = "2026-09-02"   # 行情日期，仅用于展示

# 出租容易度评分权重（合计100）
WEIGHTS = {
    "traffic": 25,   # 交通便捷(近地铁)
    "demand": 25,    # 需求与配套(商圈/医院/高校/就业)
    "age": 20,       # 房龄与硬件
    "supply": 15,    # 板块供需
    "school": 10,    # 学区
    "season": 5,     # 季节系数
}

# 综合分 = 0.6*租售比分 + 0.4*出租易度分 - 风险罚分
W_YIELD, W_EASE, W_RISK = 0.6, 0.4, 1.0

# 空置/维护年率：净回报率折减（%）
VACANCY_MAP = {"good": 3.0, "mid": 6.0, "bad": 12.0}